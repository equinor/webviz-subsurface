class PluginIds:
    class Stores:
        ENSEMBLES = "ensembles"
        TREEMODE = "tree-mode"
        #OPTIONS = "options"
        STATISTICS = "statistics"
        REALIZATION = "realization"
        FILTER = "filter"

    
    class SharedSettings:
        CONTROLS = "filter"
        OPTIONS = "options"
        FILTERS = "filters"

    class ProductionNetworkID:
        GROUP_NAME = "Production-Network"
        GROUP_TREE = "group-tree"