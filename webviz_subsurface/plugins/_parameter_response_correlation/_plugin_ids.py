class PluginIds:
    # pylint: disable=too-few-public-methods
    class Stores:
        # pylint: disable=too-few-public-methods

        INITIAL_PARAMETER = "initial-parameter"

    class SharedSettings:
        # pylint: disable=too-few-public-methods
        FILTER = "filter"

    class ResponseID:
        # pylint: disable=too-few-public-methods
        GROUP_NAME = "Response Analyzer"

        RESPONSE_CHART = "response-chart"
